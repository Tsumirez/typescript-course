/*
  "noEmitOnError": true

  Pretty simple - if there's an error, js files are not compiled.
  Keyword "files". That's right. One error prevents compilation of all .ts files, not
  just the one/s with errors. Until all errors are dealt with, no .js output files will be
  generated
*/

console.log('read the comment in the app.ts file');